version: '2'
services:
  yaadbuzz-app:
    image: yaadbuzz
    environment:
      - _JAVA_OPTIONS=-Xmx512m -Xms256m
      - SPRING_PROFILES_ACTIVE=prod,swagger
      - MANAGEMENT_METRICS_EXPORT_PROMETHEUS_ENABLED=true
      - SPRING_DATASOURCE_URL=jdbc:postgresql://yaadbuzz-postgresql:5432/yaadbuzz
      - JHIPSTER_SLEEP=3 # gives time for other services to boot before the application
    ports:
      - 8080:8080
  yaadbuzz-postgresql:
    extends:
      file: postgresql.yml
      service: yaadbuzz-postgresql

  webserver:
    build:
      context: ./web
    restart: always
    volumes:
      - workshop_media_volume:/home/app/workshop_media
      - workshop_static_volume:/home/app/workshop_staticfiles
      - ../../../build/resources/main/static:/data/admin
      - ../../../../yaadmaan-frontend/build:/data/panel
    ports:
      - 80:80
